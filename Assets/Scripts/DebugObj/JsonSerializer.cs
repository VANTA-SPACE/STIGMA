using System.Collections.Generic;
using Core;
using Core.Level;
using UnityEngine;

namespace DebugObj {
    public class JsonSerializer : MonoBehaviour {
        public NoteData NoteData = new NoteData(NoteType.Normal, 1, NotePos.POS_1);
    }
}